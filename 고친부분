* 백엔드 (`backend/`):
       * backend/src/models/conversation/schema.ts 및 backend/src/models/conversation/conversation.ts 파일을 생성하여 Conversation 모델을 정의했습니다.
       * backend/src/models/message/schema.ts를 수정하여 conversation 필드를 추가했습니다.
       * backend/src/services/conversation.ts 파일을 생성하여 대화방 생성/가져오기 로직을 구현했습니다.
       * backend/src/routes/conversations.ts 파일을 생성하여 대화방 관련 API 엔드포인트를 정의했습니다.
       * backend/src/routes/users.ts 파일을 생성하여 모든 사용자 목록을 가져오는 API 엔드포인트를 정의했습니다.
       * backend/index.ts에 conversationsRouter와 usersRouter를 추가했습니다.
       * backend/src/sockets/index.ts를 수정하여 귓속말 메시지를 처리하고, conversationId를 기준으로 메시지를 저장하고 로드하도록 했습니다.
   * 프론트엔드 (`frontend/`):
       * frontend/src/components/Chat/Chat.tsx를 수정하여:
           * User 인터페이스를 추가했습니다.
           * users, selectedUser, conversationId 상태를 추가했습니다.
           * useEffect 훅을 사용하여 모든 사용자 목록을 가져오도록 했습니다.
           * handleUserSelect 함수를 구현하여 귓속말 대상을 선택하고 대화방을 생성/가져오도록 했습니다.
           * 메시지 전송 시 conversationId를 함께 보내도록 handleSubmit 함수를 수정했습니다.
           * 메시지 로드 시 conversationId를 기준으로 메시지를 가져오도록 useEffect 훅을 수정했습니다.
           * handleClearChat 함수도 conversationId를 고려하도록 수정했습니다.
           * 귓속말 대상을 선택하는 <select> 요소를 추가했습니다.
           * 메시지 입력 필드의 placeholder를 동적으로 변경하도록 했습니다.
       * frontend/src/components/Chat/Chat.module.scss에 귓속말 UI를 위한 스타일을 추가했습니다.


==========

 * 백엔드 (`backend/`):
       * backend/src/models/message/schema.ts를 수정하여 file 필드를 추가했습니다.
       * multer를 설치하고 backend/src/routes/files.ts 파일을 생성하여 파일 업로드 및 다운로드 엔드포인트를 정의했습니다.
       * backend/index.ts에 filesRouter를 추가하고, uploads 디렉토리를 정적 파일 서비스하도록 설정했습니다.
       * backend/src/sockets/index.ts를 수정하여 파일 메시지를 처리하고, file 정보를 포함하여 클라이언트로 전송하도록 했습니다.
   * 프론트엔드 (`frontend/`):
       * frontend/src/components/Chat/Chat.tsx를 수정하여:
           * Message 인터페이스에 file 필드를 추가했습니다.
           * handleFileUpload 함수를 구현하여 파일 업로드를 처리하고 백엔드 API를 호출하도록 했습니다.
           * 파일 선택 버튼과 숨겨진 파일 입력 필드를 추가했습니다.
           * 메시지 렌더링 시 file 필드가 존재하면 파일 다운로드 링크를 표시하도록 했습니다.
       * frontend/src/components/Chat/Chat.module.scss에 파일 업로드 UI를 위한 스타일을 추가했습니다.


       ✦ 네, Context API를 사용하도록 프론트엔드 상태 관리를 리팩토링한 순서는 다음과 같습니다.

   1. 새로운 Context 파일 생성:
       * frontend/src/context/AuthContext.tsx 파일을 생성하여 사용자 인증 상태(username)를 관리하는 AuthContext와 AuthProvider, 그리고 useAuth 훅을
         정의했습니다.
       * frontend/src/context/SocketContext.tsx 파일을 생성하여 Socket.IO 인스턴스와 연결 상태를 관리하는 SocketContext와 SocketProvider, 그리고 useSocket
         훅을 정의했습니다.

   2. 기존 훅 및 소켓 파일 삭제:
       * 더 이상 필요 없는 frontend/src/hooks/useAuth.tsx 파일을 삭제했습니다.
       * 더 이상 필요 없는 frontend/src/hooks/useSocket.tsx 파일을 삭제했습니다.
       * 더 이상 필요 없는 frontend/src/socket.tsx 파일을 삭제했습니다.

   3. 애플리케이션 진입점 수정 (`frontend/src/main.tsx`):
       * App 컴포넌트를 AuthProvider와 SocketProvider로 감싸서, 애플리케이션 전역에서 인증 및 소켓 상태에 접근할 수 있도록 했습니다.

   4. 컴포넌트 훅 임포트 경로 수정:
       * frontend/src/pages/Layout/Layout.tsx 파일에서 useAuth 훅의 임포트 경로를 ../../context/AuthContext로 변경했습니다.
       * frontend/src/components/Chat/Chat.tsx 파일에서 useSocket 훅의 임포트 경로를 ../../context/SocketContext로, useAuth 훅의 임포트 경로를
         ../../context/AuthContext로 변경했습니다.

  이 과정을 통해 Context API를 활용한 전역 상태 관리가 구현되었습니다.


  ========
  
   * 실시간 메시지: 여러 사용자가 소켓을 이용해 실시간으로 메시지를 주고받아야 합니다.
   * 채팅 내역 유지: 새로고침이나 재접속 시에도 이전 채팅 내역이 보여야 합니다.
   * 채팅 내역 삭제: 채팅 내역을 지울 수 있는 기능이 필요합니다.
   * 귓속말: 1:1 메시지 기능이 필요합니다.
   * 파일 공유: 메시지를 통해 파일을 공유하고, 귓속말을 통해서도 파일을 공유할 수 있어야 합니다.




  * `backend/src/sockets/index.ts`: 백엔드 웹소켓 서버 로직을 수정하여 메시지를 데이터베이스에 저장하고, 과거 메시지를 불러와 클라이언트에 전송하는 기능을
     추가했습니다.
   * `frontend/src/components/Chat/Chat.tsx`: 프론트엔드 채팅 컴포넌트를 수정하여 서버로부터 과거 메시지를 로드하고, 새로운 메시지를 수신하여 표시하도록 했습니다.
   * `frontend/src/components/Chat/Chat.module.scss`: 채팅 컴포넌트의 스타일을 추가했습니다.

-----------------------------------
<전체삭제>
  * `backend/src/services/message.ts`: clearUserMessages 함수를 추가하여 특정 사용자의 모든 메시지를 삭제하는 로직을 구현했습니다.
   * `backend/src/routes/messages.ts`: /messages/clear 엔드포인트를 추가하여 DELETE 요청을 처리하고, clearUserMessages 함수를 호출하여 메시지를 삭제합니다.
   * `backend/index.ts`: messagesRouter를 추가하여 /messages 경로로 들어오는 요청을 처리하도록 했습니다.
   * `frontend/src/components/Chat/Chat.tsx`: "채팅 내역 삭제" 버튼을 추가하고, handleClearChat 함수를 구현하여 백엔드 API를 호출하고 프론트엔드 채팅 내역을
     초기화하도록 했습니다.
   * `frontend/src/components/Chat/Chat.module.scss`: clearChatButton에 대한 스타일을 추가했습니다.

-----------------
<개별삭제>
* `backend/src/services/message.ts`: deleteMessage 함수를 추가하여 특정 메시지 ID와 사용자 ID를 받아 해당 메시지를 삭제하는 로직을 구현했습니다.
   * `backend/src/routes/messages.ts`: /messages/:messageId 엔드포인트를 추가하여 DELETE 요청을 처리하고, deleteMessage 함수를 호출하여 메시지를 삭제합니다.
   * `frontend/src/components/Chat/Chat.tsx`:
       * useAuth 훅을 사용하여 현재 로그인한 사용자의 username을 가져왔습니다.
       * Message 인터페이스에 _id 필드를 추가했습니다.
       * 각 메시지를 렌더링할 때 msg._id를 key로 사용하도록 변경했습니다.
       * loggedInUser === msg.user 조건을 사용하여 현재 로그인한 사용자가 보낸 메시지에만 "삭제" 버튼이 표시되도록 했습니다.
       * handleDeleteMessage 함수를 구현하여 삭제 버튼 클릭 시 백엔드 API를 호출하고, 성공 시 프론트엔드에서 해당 메시지를 제거하도록 했습니다.
       * "채팅 내역 삭제" 버튼의 텍스트를 "채팅 내역 전체 삭제"로 변경했습니다.
   * `frontend/src/components/Chat/Chat.module.scss`: deleteMessageButton에 대한 스타일을 추가했습니다.

